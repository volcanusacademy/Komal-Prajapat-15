// StudentCard.js
import React from 'react';
import './styles.css'; // Import the CSS file

const StudentCard = ({ student }) => {
  return (
    <div className="student-card">
      {/* Your existing StudentCard component code */}
    </div>
  );
};

export default StudentCard;
